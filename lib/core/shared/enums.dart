enum ThemeType { light, dark }

enum AuthProvider {
  google,
  facebook,
  apple,
  phone,
  email,
  unknown,
}

enum FirebaseCollections {
  users,
  cards,
  transactions,
  profile,
  reasons,
  fare,
  googleAdsUnit,
  ads,
}
